{
  "name": "opencode-tamu-finish-fix",
  "version": "1.0.0",
  "description": "Fixes incorrect finish_reason values from OpenAI-compatible proxies that return 'stop' when tool calls are present",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    "./server": "./src/index.ts"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.3.10"
  },
  "devDependencies": {
    "@opencode-ai/plugin": ">=1.3.10",
    "typescript": "^5.0.0"
  }
}

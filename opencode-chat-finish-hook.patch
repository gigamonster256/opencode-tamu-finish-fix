diff --git a/packages/opencode/src/session/processor.ts b/packages/opencode/src/session/processor.ts
index b632a61a1..f85d85314 100644
--- a/packages/opencode/src/session/processor.ts
+++ b/packages/opencode/src/session/processor.ts
@@ -52,6 +52,7 @@ export namespace SessionProcessor {
     needsCompaction: boolean
     currentText: MessageV2.TextPart | undefined
     reasoningMap: Record<string, MessageV2.ReasoningPart>
+    toolCallsHistory: Array<{ id: string; name: string; input: unknown }>
   }
 
   type StreamEvent = Event
@@ -95,6 +96,7 @@ export namespace SessionProcessor {
           needsCompaction: false,
           currentText: undefined,
           reasoningMap: {},
+          toolCallsHistory: [],
         }
         let aborted = false
 
@@ -173,6 +175,7 @@ export namespace SessionProcessor {
               }
               const match = ctx.toolcalls[value.toolCallId]
               if (!match) return
+              ctx.toolCallsHistory.push({ id: value.toolCallId, name: value.toolName, input: value.input })
               ctx.toolcalls[value.toolCallId] = yield* session.updatePart({
                 ...match,
                 tool: value.toolName,
@@ -267,6 +270,26 @@ export namespace SessionProcessor {
                 metadata: value.providerMetadata,
               })
               ctx.assistantMessage.finish = value.finishReason
+              const finishOutput = { reason: value.finishReason ?? "unknown" }
+              yield* plugin.trigger(
+                "chat.finish",
+                {
+                  sessionID: ctx.sessionID,
+                  messageID: ctx.assistantMessage.id,
+                  agent: ctx.assistantMessage.agent,
+                  model: ctx.model,
+                  providerID: ctx.model.providerID,
+                  finishReason: value.finishReason,
+                  toolCalls: ctx.toolCallsHistory,
+                  usage: {
+                    promptTokens: usage.tokens.input,
+                    completionTokens: usage.tokens.output,
+                    totalTokens: usage.tokens.total,
+                  },
+                },
+                finishOutput,
+              )
+              ctx.assistantMessage.finish = finishOutput.reason
               ctx.assistantMessage.cost += usage.cost
               ctx.assistantMessage.tokens = usage.tokens
               yield* session.updatePart({
diff --git a/packages/plugin/src/index.ts b/packages/plugin/src/index.ts
index a264cf5aa..028111560 100644
--- a/packages/plugin/src/index.ts
+++ b/packages/plugin/src/index.ts
@@ -262,4 +262,26 @@ export interface Hooks {
    * Modify tool definitions (description and parameters) sent to LLM
    */
   "tool.definition"?: (input: { toolID: string }, output: { description: string; parameters: any }) => Promise<void>
+  /**
+   * Called when a step finishes, before the finish reason is committed.
+   * Allows plugins to inspect and override the finish reason.
+   * Useful for fixing incorrect finish_reason values from upstream providers.
+   */
+  "chat.finish"?: (
+    input: {
+      sessionID: string
+      messageID: string
+      agent: string
+      model: Model
+      providerID: string
+      finishReason: string | undefined
+      toolCalls: Array<{ id: string; name: string; input: unknown }>
+      usage: {
+        promptTokens: number
+        completionTokens: number
+        totalTokens: number
+      }
+    },
+    output: { reason: string },
+  ) => Promise<void>
 }
